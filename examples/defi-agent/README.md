This agent autonomously swaps tokens and deploys capital across DeFi protocols. Without policy, it could drain a treasury in one token transfer, route funds to sanctioned addresses, or accept undefined job types from an on-chain work marketplace. This warranty.md caps native ETH and USDC separately, pins USDC contract addresses, allowlists chains, blocks sanctioned addresses, and requires an allowlisted job type on every governed intent.
