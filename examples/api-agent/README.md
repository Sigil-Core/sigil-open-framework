This agent makes autonomous API calls to external services in a LangChain or ELIZA pipeline. Without policy, it could POST sensitive data to arbitrary endpoints, SSRF into cloud metadata, or email results to an unbounded recipient list. This warranty.md blocks requests to internal networks and metadata endpoints, prevents credential leakage, bounds email recipients, and requires an allowlisted job type on every governed intent.
