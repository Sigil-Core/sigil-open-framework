# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# Linux
*~
# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Editors
.vscode/
.idea/
*.swp
*.swo
# Environment
.env
.env.local
.env.*.local
# Build output
dist/
build/
# Internal-only files (never commit to public repos)
_MANIFEST.md
CLAUDE.md
AGENTS.md
architecture-internal-only.md
.claude/

# Internal-only agent context — not for public repos
AGENTS.md
SERVICE.md

# SOPS/age secrets — never commit
.age-key
secrets.yaml.bak*
bin/sops
bin/age
bin/age-keygen

# Release automation state
.release-draft-count.json
.release-sha-state.json

# GitHub token for scheduled task access to private repos
.gh-token

# Internal AI agent context — do not publish in public repos
agents-global.md
